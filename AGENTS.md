# SoraSense 開発ルール

## 学習を優先したペアプログラミング

このプロジェクトでは、利用者が仕組みを理解しながら自分で実装することを最優先とする。
機能追加、修正、リファクタリングなどの実装依頼では、Codexが最初から完成コードを実装してはならない。

### 基本の進め方

1. 要件、既存コード、関連ドキュメントを確認する。
2. 実装を、1回で理解・実行・確認できる小さな学習ステップに分ける。
3. 各ステップの開始時に、次の内容を説明する。
   - このステップの目的
   - 学ぶ仕組みや概念
   - 変更対象のファイルと、そのファイルを変更する理由
   - 利用者が実装する具体的な範囲
   - 完了条件と確認方法
4. 原則として一度に1ステップだけ提示し、利用者の実装を待つ。
5. 利用者が実装したコードを、正しさ、要件適合性、安全性、読みやすさ、テストの観点でレビューする。
6. レビューでは、問題箇所だけでなく、問題になる理由と修正の考え方を説明する。
7. ステップ完了後に、今回学んだことを短く整理してから次へ進む。

### 支援の段階

利用者が詰まった場合は、次の順番で支援する。利用者が明示的に次の段階を求めるまでは、先の段階へ進まない。

1. ヒント: 考え方、確認場所、関連概念だけを示す。
2. 部分例: 最小限の擬似コードまたはコード断片を示す。
3. 完成例: 利用者が明示的に希望した場合に限り、完成コードを示す、またはCodexが実装する。

### Codexが実行してよい作業

- 読み取り専用の調査、既存コードの説明、設計案の作成
- 利用者が書いたコードのレビュー
- テスト、リンター、型チェックなどの検証（安全かつ既存環境で実行可能な場合）
- 利用者が明示的に依頼したドキュメントの作成・更新
- 利用者が明示的に完成実装を依頼した範囲のコード変更

### 完成コードを先に提示しないための注意

- 学習対象となる実装を、説明なしにファイルへ書き込まない。
- 次のステップ以降の完成コードを先回りして提示しない。
- 大きな変更を一括で提示せず、動作確認可能な単位へ分割する。
- 単なるコピー作業にせず、重要な設計判断では理由と代替案を示す。
- 利用者のコードに問題があっても、直ちに全面的に書き換えず、まず自力で直せる説明を行う。

### 例外

利用者が「今回はCodexが実装して」「完成コードを作って」など、実装代行を明示的に依頼した場合は、その依頼範囲に限ってCodexが実装してよい。
その場合も、主要な変更点、採用理由、確認方法を説明し、学習できる形で引き渡す。

緊急のセキュリティ問題、データ損失の危険、または利用者の環境を壊す可能性を発見した場合は、学習手順より安全確保を優先して警告する。

## 言語

説明、手順、レビューは原則として日本語で行う。コード上の命名は、使用技術の一般的な慣習に従い英語とする。

### コード内の説明

- Pythonのモジュール、クラス、関数およびメソッドのdocstringは原則として日本語で記載する。
- コメントも原則として日本語で記載し、外部仕様の固有表現や一般的な技術用語は無理に翻訳しない。
- 変数、関数、クラスなどの識別子は、Pythonの一般的な慣習に従って英語で命名する。
- コードから分かる「何をするか」は命名と構造で表し、コードだけでは分からない「なぜその方式か」「どの制約を守るか」「安全上何に注意するか」をdocstringまたはコメントで説明する。
- モジュールのdocstringには、必要に応じてそのファイルの責務、他の層との関係および担当しない処理を記載する。
- クラス、Enumおよび定数には、名前だけでは責務、状態、ライフサイクル、単位または値の根拠が分からない場合に説明を付ける。
- 複雑な処理、例外変換、セキュリティ境界および並行処理には、処理順序や採用理由が分かる説明を付ける。
- コードをそのまま日本語へ言い換えるだけのコメント、変更のたびに陳腐化しやすい逐行コメント、および設計書と同じ内容の過剰な重複は避ける。

## 要件定義と設計書の分離・整合性

文書は、要件定義書、基本設計書、詳細設計書の順に具体化する。下位文書は上位文書を根拠とし、上位文書で確定した内容と矛盾してはならない。

### 各文書に記載する内容

- 要件定義書は「何を満たすか」を定義し、目的、対象範囲、利用者、必要な機能、品質条件、業務上・外部上の制約および受入条件を記載する。技術、構成または実現方式は、利用者が変更不能な前提条件または必須制約として明示した場合だけ記載し、設計案ではなく制約であることを明記する。
- 基本設計書は「要件をどのような全体構造で実現するか」を定義し、システム構成、構成要素の責務、主要な技術選定、外部インターフェースの概要、画面・機能の構成と遷移、論理データ構造、セキュリティ・運用・テストの基本方針、および要件とのトレーサビリティを記載する。
- 詳細設計書は「各構成要素を実装できる粒度でどう作るか」を定義し、モジュール・クラス・関数の責務、処理手順とシーケンス、APIの要求・応答・エラーの詳細、物理データ構造、入力検証、アルゴリズム、設定値、ログ、例外処理およびテストケースを記載する。
- 基本設計書には、内部処理の詳細、物理テーブルの全項目、関数単位の仕様など、詳細設計に属する情報を原則として記載しない。ただし、システム全体の方式判断または要件適合性を説明するために必要な範囲は記載できる。

### 文書間の整合性

- 基本設計書を新規作成または変更する場合は、要件定義書との整合性および要件トレーサビリティを確認する。
- 詳細設計書を新規作成または変更する場合は、要件定義書に加えて基本設計書との整合性を確認し、基本設計で定めた構成、責務、インターフェース、データおよび品質方針を満たすことを確認する。
- 詳細設計が基本設計と両立しない場合は、詳細設計だけで新しい方式を確定しない。実現方式を変える場合は基本設計書を同じ作業内で更新し、利用者の振る舞い、提供機能、対象範囲、品質条件または受入条件まで変わる場合は要件定義書も更新する。
- 利用者の判断が必要な要件変更は勝手に確定せず、不整合の内容と選択肢を説明して確認する。ただし、利用者が要件変更を明示した場合は、その範囲を一括して反映する。
- 同じ説明を複数文書へ重複して記載せず、その内容を決定する文書を正とする。下位文書では上位文書の要件ID、設計項目または参照版数を示し、実装に必要な追加情報だけを記載する。

### 文書版数

- Gitの追跡対象である要件定義書、基本設計書および詳細設計書の文書版数には、`MAJOR.MINOR.PATCH` 形式のセマンティックバージョニングを適用する。各文書をGitにコミットする際は、変更内容に応じてその文書の版数を更新し、上位の番号を更新した場合は下位の番号を0へ戻す。
  - `MAJOR` は、初期リリース後に確定済みの要件または設計との後方互換性を失う変更に使用する。
  - `MINOR` は、後方互換性を保った要件または設計の追加・拡張に使用する。初期リリース前の `0.x.y` では、文書の意味に影響する追加・削除・変更にも使用する。
  - `PATCH` は、文書の意味を変えない誤記修正、表現の明確化、書式調整、参照版数または要件トレーサビリティの修正に使用する。
  - `0.0.0` は版数管理開始時点の暫定版とする。
- 上位文書の版数を変更する場合は、下位文書の参照版数とトレーサビリティを同じコミットで同期する。下位文書だけを変更する場合は、変更していない上位文書の版数を更新しない。

## Gitコミット

### コミットメッセージ

- コミットメッセージは日本語で記載する。
- 1コミットの変更内容を簡潔に表現する。
- 形式は`<プレフィックス>: <日本語の要約>`とする。
- 要約には末尾の句点を付けず、変更した内容が分かる表現を用いる。
- 複数種類の変更を含む場合は、主目的に最も近いプレフィックスを選ぶ。主目的を1つに定められない場合は、コミットを分割する。

### プレフィックス

| プレフィックス | 用途 |
|---|---|
| `feat` | 機能の追加・変更 |
| `fix` | 不具合の修正 |
| `docs` | ドキュメントだけの変更 |
| `refactor` | 機能を変えないコード構造の改善 |
| `test` | テストの追加・変更 |
| `chore` | 設定、依存関係、開発環境などの保守作業 |
| `style` | 動作に影響しない書式変更 |
| `perf` | 性能改善 |

例: `docs: 基本設計と詳細設計の分離ルールを追加`
